{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "🐍 Debug FastAPI (no uv)",
        "type": "debugpy",
        "request": "launch",
        "module": "uvicorn",
        "args": [
          "app.main:app",
          "--env-file=.env",
          "--reload"
        ],
        "console": "integratedTerminal",
        "envFile": "${workspaceFolder}/.env",
        "cwd": "${workspaceFolder}"
      }
    ]
  }